[x] fix why add account card under transactions/add is having a centered text
[x] update asset growth pill calculations refactoring +0%ytd and +infinity
[x] build Subscription backend — Prisma model + migration `20260719070857_add_subscription` + 3-layer route `/api/subscriptions` (schema/service/controller/route) + mount in `src/server/index.ts`
[x] build Budgets frontend (`src/app/budget/page.tsx`) — monthly + daily summary cards, spending-streams horizontal bar chart (per-category expense this month + budget markers), budgets list with mini progress bars, 3-step add-budget wizard in Dialog, bottom-sheet detail + confirm delete
[x] build Subscriptions frontend — subscription list with next-billing-date, single-dialog add form, bottom-sheet detail + confirm delete
[x] add `src/lib/budget.ts` helpers (periodLabel, periodStartDate, nextBillingDate, startOfToday, startOfMonth) + `src/lib/category-icon.tsx` (categoryIcon → ReactElement, lint-safe)
[x] update ARCHITECTURE.md (§19 Budgets UI, Subscription model/schema/route, repo structure), UI_DESIGN.md (§14 Budgets UI patterns, §13 where-to-look), AGENTS.md (resources + conventions), README.md (features done + structure)
[x] add `User.plus Boolean @default(false)` to Prisma schema + migration `20260719121457_add_plus_to_user`
[x] build `/api/user` 3-layer route (routes/user.ts + controllers/user.ts + services/user.ts + schemas/user.ts) — GET returns `{ plus: boolean }`, PATCH updates plus flag (required boolean schema, rejects Prisma op objects). Renamed from inconsistent `/api/plus` + `userManage.ts` naming.
[x] build profile page (`src/app/profile/page.tsx`) — black identity card (avatar initials, email + tier, membership id) + conditional Plus member card / upgrade card (one `variant="success"` CTA per surface). SignOutButton moved here from sidebar.
[x] build `UpgradePlusButton` — two-mode: `!plus` → opens `PlusPaymentWizard`; `plus=true` → direct PATCH downgrade with loading + error pill
[x] build mock Midtrans QRIS checkout — `src/lib/midtrans.ts` mock client (createQrisTransaction, getTransactionStatus, simulatePayment, verifyWebhookSignature, parseWebhookNotification — every function marked `// === MOCK ===`), `/api/plus` 3-layer route (checkout, status, simulate-payment, public webhook), `PlusOrder` Prisma model + migration `20260720000000_add_plus_order`, `PlusPaymentWizard` 3-step Dialog (package → QR via qrcode.react → success) with 3s polling + "I've paid" simulate button
[x] write tests — `schemas/user.test.ts`, `services/user.test.ts`, `controllers/user.test.ts`, `midtrans.test.ts`, `schemas/plus.test.ts`, `services/plus.test.ts`, `controllers/plus.test.ts`, `plus-payment-wizard.test.tsx`, `upgradePlusButton.test.tsx` (updated), `account-tab.test.tsx` (fixed for async RSC). 479 tests passing.
[x] update ARCHITECTURE.md (§20 Profile & Plus UI, mock Midtrans flow, PlusOrder model), UI_DESIGN.md (§14.7 Plus wizard, §4.5 sidebar no SignOutButton, §13 where-to-look), AGENTS.md (`/api/plus` resource, midtrans mock note, UpgradePlusButton), README.md (features, tech stack qrcode.react, test count 479, structure)
[x] docs reconciliation + README demo video — added a `<video controls muted loop playsinline>` demo (Vercel-Blob `budgieDemo.mp4`, same clip as the landing `AutoVideo`) to `README.md` top block. Reconciled all four canonical docs against the Jul 26–27 landing-page work they had silently missed: added a "Public landing page" Features bullet + full project-structure update (root `page.tsx`, `components/landing/`, `public/videos/`, complete `lib/` incl. `auth-client`/`dashboard`/`font-size`) + deep-dive refs in `README.md`; added a "Public landing page" convention note + the three missing `src/lib/*` helper bullets in `AGENTS.md`; appended `ARCHITECTURE.md` §21 "Landing Page (frontend)" (composition, `AutoVideo` `<video>` pattern, `public/videos/` drop-in, SEO/JSON-LD, gotchas) + a note on the two sequential `add_plus_order` migrations; appended `UI_DESIGN.md` §16 "Landing Page — Marketing Surface" + new `src/components/landing/*` rows in §13 where-to-look. Fixed drift: test count 479→480 (badge + 2 body spots), Tailwind `4.3.x`→`^4` (matches `package.json`), component count "28+"→"46 (29 root + 17 landing)". Deleted two dead, unreferenced stubs (`src/server/services/accounts.ts` 0-byte + `src/server/schemas/account.ts`) and cleaned their lines out of `ARCHITECTURE.md` §3 structure tree. `bun run lint && typecheck && test` green (480/480, 44 suites) — md-only changes don't run, but stub deletion re-verified.
