[x] fix why add account card under transactions/add is having a centered text
[x] update asset growth pill calculations refactoring +0%ytd and +infinity
[x] build Subscription backend — Prisma model + migration `20260719070857_add_subscription` + 3-layer route `/api/subscriptions` (schema/service/controller/route) + mount in `src/server/index.ts`
[x] build Budgets frontend (`src/app/budget/page.tsx`) — monthly + daily summary cards, spending-streams horizontal bar chart (per-category expense this month + budget markers), budgets list with mini progress bars, 3-step add-budget wizard in Dialog, bottom-sheet detail + confirm delete
[x] build Subscriptions frontend — subscription list with next-billing-date, single-dialog add form, bottom-sheet detail + confirm delete
[x] add `src/lib/budget.ts` helpers (periodLabel, periodStartDate, nextBillingDate, startOfToday, startOfMonth) + `src/lib/category-icon.tsx` (categoryIcon → ReactElement, lint-safe)
[x] update ARCHITECTURE.md (§19 Budgets UI, Subscription model/schema/route, repo structure), UI_DESIGN.md (§14 Budgets UI patterns, §13 where-to-look), AGENTS.md (resources + conventions), README.md (features done + structure)
[x] add `User.plus Boolean @default(false)` to Prisma schema + migration `20260719121457_add_plus_to_user`
[x] build `/api/user` 3-layer route (routes/user.ts + controllers/user.ts + services/user.ts + schemas/user.ts) — GET returns `{ plus: boolean }`, PATCH updates plus flag (required boolean schema, rejects Prisma op objects). Renamed from inconsistent `/api/plus` + `userManage.ts` naming.
[x] build profile page (`src/app/profile/page.tsx`) — black identity card (avatar initials, email + tier, membership id) + conditional Plus member card / upgrade card (one `variant="success"` CTA per surface). SignOutButton moved here from sidebar.
[x] build `UpgradePlusButton` — two-mode: `!plus` → opens `PlusPaymentWizard`; `plus=true` → direct PATCH downgrade with loading + error pill
[x] build mock Midtrans QRIS checkout — `src/lib/midtrans.ts` mock client (createQrisTransaction, getTransactionStatus, simulatePayment, verifyWebhookSignature, parseWebhookNotification — every function marked `// === MOCK ===`), `/api/plus` 3-layer route (checkout, status, simulate-payment, public webhook), `PlusOrder` Prisma model + migration `20260720000000_add_plus_order`, `PlusPaymentWizard` 3-step Dialog (package → QR via qrcode.react → success) with 3s polling + "I've paid" simulate button
[x] write tests — `schemas/user.test.ts`, `services/user.test.ts`, `controllers/user.test.ts`, `midtrans.test.ts`, `schemas/plus.test.ts`, `services/plus.test.ts`, `controllers/plus.test.ts`, `plus-payment-wizard.test.tsx`, `upgradePlusButton.test.tsx` (updated), `account-tab.test.tsx` (fixed for async RSC). 479 tests passing.
[x] update ARCHITECTURE.md (§20 Profile & Plus UI, mock Midtrans flow, PlusOrder model), UI_DESIGN.md (§14.7 Plus wizard, §4.5 sidebar no SignOutButton, §13 where-to-look), AGENTS.md (`/api/plus` resource, midtrans mock note, UpgradePlusButton), README.md (features, tech stack qrcode.react, test count 479, structure)
[x] docs reconciliation + README demo video — added a `<video controls muted loop playsinline>` demo (Vercel-Blob `budgieDemo.mp4`, same clip as the landing `AutoVideo`) to `README.md` top block. Reconciled all four canonical docs against the Jul 26–27 landing-page work they had silently missed: added a "Public landing page" Features bullet + full project-structure update (root `page.tsx`, `components/landing/`, `public/videos/`, complete `lib/` incl. `auth-client`/`dashboard`/`font-size`) + deep-dive refs in `README.md`; added a "Public landing page" convention note + the three missing `src/lib/*` helper bullets in `AGENTS.md`; appended `ARCHITECTURE.md` §21 "Landing Page (frontend)" (composition, `AutoVideo` `<video>` pattern, `public/videos/` drop-in, SEO/JSON-LD, gotchas) + a note on the two sequential `add_plus_order` migrations; appended `UI_DESIGN.md` §16 "Landing Page — Marketing Surface" + new `src/components/landing/*` rows in §13 where-to-look. Fixed drift: test count 479→480 (badge + 2 body spots), Tailwind `4.3.x`→`^4` (matches `package.json`), component count "28+"→"46 (29 root + 17 landing)". Deleted two dead, unreferenced stubs (`src/server/services/accounts.ts` 0-byte + `src/server/schemas/account.ts`) and cleaned their lines out of `ARCHITECTURE.md` §3 structure tree. `bun run lint && typecheck && test` green (480/480, 44 suites) — md-only changes don't run, but stub deletion re-verified.
[x] full test-suite expansion (480 → 854 tests, 44 → 74 suites) — closed every remaining gap: **new** `src/lib/budget.test.ts` (period labels incl. 0/negative/fractional, next-billing-date boundaries, month/leap-year crossings), `src/lib/category-icon.test.tsx` (every category → icon, fallback, TYPE_ICON), subscriptions 3-layer (`services/`/`schemas/`/`controllers/subscriptions.test.ts` — duplicate-name clash incl. rename `NOT {id}`, schema defaults/no-defaults, 400/404/409/204 mapping), budget & subscription component suites (`budgets-list`, `add-budget-dialog` 3-step wizard, `budget-detail-sheet`, `budget-summary-cards`, `subscription-list`, `subscription-detail-sheet`, `add-subscription-dialog`, `spending-streams-chart`, `page-shell`), full landing coverage (116 tests: `auto-video` poster/reduced-motion/lazy-sources, `animated-counter` eased rAF frames, `scroll-progress` clamping, `reveal`, `hero-preview` live-pool rotation, marquee/faq/pricing/nav/footer/feature-grid/vignettes/privacy-spotlight/video-showcase/showcase content suites, `mock-data.ts` integrity) + **new shared stubs** `src/test-utils/browser-mocks.ts` (stubIntersectionObserver, stubMatchMedia, stubRequestAnimationFrame, setWindowScrollY, setViewport). **Deepened** existing: format (NaN/∞/large/rounding/invalid dates), midtrans (15-min expiry + auto-expire via fake timers, EMVCo tag-54 length prefix, copy-safety), transactions service (ownership scoping asserts, exact-empties allowed, missing-dest delete), plus-payment-wizard (status-polling suite: settlement/expire/cancel, polling stops on terminal — caught & fixed a `mockResolvedValueOnce` cross-test leak + userEvent-vs-fake-timers hang). Docs reconciled: README (badge 854, suite table, test-utils + async-RSC-exclusion note), AGENTS.md (test count + Testing conventions incl. browser-mocks/act/fireEvent/once-queue rules), ARCHITECTURE.md (§12.4 Testing section). `bun run lint` (0 errors) && `typecheck` && `test` green (854/854, 74 suites).
[x] landing de-AI-slop pass — modern motion layer on top of the untouched section skeleton (hero → stats → marquee → video → features → showcase → vignettes → privacy → pricing → faq → CTA → footer; `src/app/page.tsx` untouched, it's metadata only). New "use client" primitives (SSR-safe, CSS-var driven, touch ignored, `motion-reduce` gated): `spotlight.tsx` (mouse-follow green radial glow via --spot-x/y), `tilt.tsx` (±6° perspective tilt for the hero preview), `magnetic.tsx` (CTA nudge ≤4px/150ms), `hero-headline.tsx` (staggered line-mask reveal + "Control" in brand green — Geist only, no webfont). Upgrades: index (live-pulse eyebrow dot, green halo, scroll cue, per-tile stat stagger, magnetic CTAs incl. final CTA), feature-grid (SpotlightCard + hover lift + icon micro-scale), pricing (SpotlightCard both tiers, green glow hover, perkPulse on the 50% pill), marquee (green-fill pill hover, white dot/label), auto-video glyph hover scale, faq (grid-template-rows 0fr→1fr smooth accordion). 25 new tests (spotlight/tilt/magnetic/hero-headline: pointer-move vars, touch ignore, reset, rect-stubbed math). Docs: UI_DESIGN.md §16 (interaction primitives table + usage rules, file count 17→21), ARCHITECTURE.md §21 tree + §12.4, AGENTS.md landing bullet + counts, README badge/structure (46→50 components). `bun run lint` (0 errors) && `typecheck` && `test` green (879/879, 78 suites).
[x] landing test rewrite + smooth scroll — user reworked `landing/index.tsx` (removed eyebrow pill + stats strip, punchier copy: "distraction free dashboard", "Get started for free", "See Budgie in motion"). Split `LandingPage` (async RSC, session fetch) from the sync `LandingPageView { session }` (the AccountTab/AccountTabView pattern) so the whole page is testable in jsdom — new `landing/index.test.tsx` (10 tests: headline/copy/CTAs, stats-strip absence, full section composition in order, pricing, final CTA, footer, session-conditional nav with footer-link counting). Added smooth scroll: `html { scroll-behavior: smooth }` in `globals.css` + `prefers-reduced-motion` guard (nav `#motion`/`#features` anchors glide). Cleaned the now-unused `STATS`/`AnimatedCounter` imports from `index.tsx`. Hero headline copy is the user's "Your money, / **Your Control**" (green-accented "Control") — confirmed intentional; tests + UI_DESIGN §16 updated to match. Docs synced: UI_DESIGN.md §16 (stat strip parked, testability split, smooth scroll), ARCHITECTURE.md §21 + §12.4 (889/79), AGENTS.md landing bullet + counts, README badge/table/exclusion note. `bun run lint` (0 errors) && `typecheck` && `test` green (889/889, 79 suites).
[x] super smooth scroll — replaced CSS `scroll-behavior: smooth` with **Lenis** (`bun add lenis@1.3.26`) via a new `"use client"` layer `src/components/landing/smooth-scroll.tsx` mounted in `LandingPageView`: eased rAF loop (`duration: 1.15`), intercepts hash clicks (`#section` + `/#section` — the nav/footer/hero anchors were upgraded to `/#section` hrefs so they glide from any page) and scrolls with a −88px sticky-nav offset; on mount it settles a deep-link `/#hash` from the URL with the same glide then strips it via `history.replaceState`. Never mounts under `prefers-reduced-motion` (native jumps); `globals.css` now carries Lenis's recommended `.lenis` styles instead of the CSS smooth rule. 10 new tests in `smooth-scroll.test.tsx` (Lenis mocked via `vi.hoisted` class: reduced-motion skip, rAF loop, click interception incl. `/#` form, missing-target no-op, external-link no-op, deep-link settle + hash strip, cleanup destroy/listener-removal). Updated nav/footer/index anchor-href tests (`#x` → `/#x`; hero anchor switched to `next/link` to satisfy `no-html-link-for-pages`). Docs: UI_DESIGN §16 (Lenis bullet + primitives row), ARCHITECTURE §2 tech table + §21 tree + counts, AGENTS.md, README tech table + badge (899/80). `bun run lint` (0 errors) && `typecheck` && `test` green (899/899, 80 suites).
[x] build the AI chat assistant — **Vercel AI SDK v7 + Google Gemini 2.5 Flash** (`bun add ai @ai-sdk/react @ai-sdk/google`). Dedicated Route Handler `src/app/api/chat/route.ts` (NOT Hono — per-route `maxDuration=30` for long AI streams, static route wins over the `[[...route]]` catch-all, same precedent as `/api/auth/*`): better-auth session → `streamText` → `toUIMessageStreamResponse()`. Backend in `src/server/chat/` — `tools.ts` (`createChatTools(userId)` factory calling existing services directly: get_balance_accounts / get_transactions / get_budgets / get_subscriptions / get_insights + create_transaction returning `{ok, transaction|error}`), `schemas.ts` (strict Zod tool schemas incl. transfer + category-type refines), `system.ts` (`buildSystemPrompt` — follows the user's language, never invents data, explicit-user-intent-before-create rule). New services: `insights.ts` (`getFinancialInsights`) + `listBudgetsWithSpent` in `budgets.ts`. **AI-call-optimized**: last-16-message window, compact/capped tool output with `count`, `stepCountIs(4)` loop bound, `maxOutputTokens: 1024`, capped `thinkingBudget` + `includeThoughts: true` (streams reasoning), `smoothStream` word-chunking, parallel tool calls. UI in `src/components/chat/` (design-matched: §7.2 rows, 3-tint system, `rounded-[20px]` cards, `stepReveal` ≤200ms, `motion-reduce`): `ChatView` (useChat + DefaultChatTransport, app-managed input since v7 dropped it), `ChatMessage` (black user pill / `#F2F2F2` assistant bubble, parts-based), `ChatThinking` (pulsing "Thinking…" → collapsible "Thought for a moment"), `ChatToolCard`/`ChatToolStatus` (running pills) + `ChatToolResult` (accounts/transactions/budgets/subscriptions/insights/create-transaction cards incl. error states), `ChatInput` (auto-grow pill + green send / Stop), `ChatEmptyState` (greeting + 4 suggestion chips). Tests (+74): chat tools/schemas/system, insights service, chat-view (useChat mocked via `vi.hoisted`), tool-result/thinking/status components. Env `GOOGLE_GENERATIVE_AI_API_KEY` in `.env` + `.env.example` + Vercel tables. Docs: ARCHITECTURE §22, UI_DESIGN §17, AGENTS.md, README (badge 921/83). `bun run lint` (0 errors) && `typecheck` && `test` green (921/921, 83 suites).
[x] chat quota fallback + token-lean pass — auto-downgrade to a lighter model when Gemini's free tier caps out (the 429 is per-model: `GenerateRequestsPerDayPerProjectPerModel-FreeTier`, 20/day on `gemini-2.5-flash`). New shared `src/lib/chat-models.ts`: `MODEL_CHAIN = ["gemini-2.5-flash", "gemini-3.5-flash-lite"]`, `resolveModel` (allowlists client-sent `body.model`, blocks injection), `isRateLimitError` (matches 429/quota/RESOURCE_EXHAUSTED/rate limit), `thinkingConfigFor` (primary: budget 256 + includeThoughts; lite: thinking off). Route reads `body.model`; `ChatView` sends `body.model` on every send/retry and auto-downgrades once on a quota error via `regenerate({ body: { model } })` + "Switched to a lighter model" notice (Sparkles, dash-free) + persists the choice in `budgie.chat.{userId}.model` (12h validity — daily quota reset restores the primary; Clear chat resets it). **Token-lean**: `prepareMessagesForApi` (`src/server/chat/prepare.ts`) — last-10 window + strips `tool-*` parts older than 2 turns and `reasoning` parts older than 1 turn; `maxOutputTokens` 1024→640; `stepCountIs(4)`→`(3)`; thinking budget 1024→256; system prompt shortened ~40% + "2-3 short sentences" rule; `get_transactions` default limit 10 (max 30) with slimmed fields (dropped id/adminFee/toAccount). Tests (+33): chat-models (7), prepare (6), storage model keys + 12h expiry (5), chat-view auto-downgrade/no-generic-downgrade/restore-lite/no-past-last (9); fixed `execute` arity typing in tools tests via a `run<O>()` helper (ToolExecuteFunction requires options + returns AsyncIterable union). Docs: ARCHITECTURE §22 (Model fallback + updated optimization list), AGENTS.md (`/api/chat` bullet + counts), README (badge 954/86, chat bullet, suite rows, lib listing), UI_DESIGN §17 (Persistence & Clear chat + lighter-model notice). `bun run lint` (0 errors) && `typecheck` && `test` green (954/954, 86 suites).
[x] chat model indicator + composer redesign — the composer (`ChatInput`) now follows the Claude/ChatGPT/Gemini anatomy on Budgie tokens: bordered card (`rounded-[24px] border-black/10 bg-white` + soft shadow, focus deepens the hairline + 2px ring), transparent auto-growing textarea (`min-h-[44px] max-h-40`, "Ask about your money…"), a bottom row inside the card with a **static model chip** on the left (Sparkles icon, green when lite + "Gemini 2.5 Flash" / "Gemini 2.5 Flash Lite" via new `MODEL_LABELS`/`modelLabel` in `src/lib/chat-models.ts`) and the circular brand-green send (`w-9 h-9 bg-[#00C610]` + ArrowUp, swaps to outline Square Stop while streaming), plus the Claude-style trademark caption below: "Budgie can make mistakes. Double-check the important numbers." (`text-[11px] text-black/35`, dash-free). ChatView slimmed the top row (removed the redundant helper sentence — the caption covers it; row now hosts the lighter-model notice left + Clear chat right) and tuned the column height calcs for the taller composer. Tests (+2): chat-view composer chip + caption + lite-chip assertions, chat-models `MODEL_LABELS`/`modelLabel` coverage + fallback. Docs: UI_DESIGN §17 (composer spec + layout block), ARCHITECTURE §22 (model chip line), README (suite rows), Arco_todo. `bun run lint` (0 errors) && `typecheck` && `test` green (956/956, 86 suites).
[x] chat column centered-narrow + composer pinned to the bottom — the `/chat` conversation now lives in a `max-w-3xl mx-auto` centered column (768px, full-width on mobile, Claude/ChatGPT/Gemini anatomy) and the composer is pinned to the viewport bottom: ChatView root is `h-[calc(100dvh-14rem)] md:h-[calc(100vh-12rem)]` (accounts for AccountTab + h1 + page paddings and clears the mobile bottom tab bar), so the message list scrolls internally (`flex-1 overflow-y-auto`) while the composer card + disclaimer stay fixed at the bottom. Docs: UI_DESIGN §17 (layout block + pinned-bottom note). `bun run lint` (0 errors) && `typecheck` && `test` green (958/958, 86 suites).
[x] chat composer translucent + double-scroll fix — the composer card is now **frosted glass** (`bg-white/70 backdrop-blur-md`, firms to `bg-white/85` on focus) over a `pointer-events-none` white gradient (`h-12 from-white to-transparent`) that fades the message list's tail into it, Claude-style. **Double scrollbar killed**: ChatView now measures its own `getBoundingClientRect().top` at runtime (`ResizeObserver` on the root + window resize listener) and sets `height: calc(100dvh - top - bottomPad)` (56px desktop / 96px mobile for tab-bar clearance) — the page never scrolls, the message list is the only scroll area, and header wrapping (AccountTab/h1) is absorbed automatically. The AccountTab + h1 header moved into the ChatView `children` slot (RSC→client slot pattern) so it sits inside the centered `max-w-3xl` column with the conversation, matching Claude/ChatGPT/Gemini. Docs: UI_DESIGN §17 (layout block + frosted card spec). `bun run lint` (0 errors) && `typecheck` && `test` green (958/958, 86 suites).
[x] chat UI revised to the Claude reference (`ui/Memulai project C++ dari nol - Claude.html`) — extracted Claude's chat anatomy and applied it on Budgie's light tokens: **(1) sticky composer in the scroll thread** (`sticky bottom-0 z-[5] pt-4` inside `overflow-y-auto [scrollbar-gutter:stable]`, with a `grow` spacer so it hugs the bottom on short chats) — messages now scroll *behind* the frosted-glass card so the `bg-[#F4F4F4]/70 backdrop-blur-md` translucency actually reads as glass; **(2) Claude message anatomy** — user messages became right-aligned `max-w-[85%] rounded-[20px] bg-[#F2F2F2]` neutral-gray bubbles (was black), assistant text is full-column-width with **no bubble** in `font-serif text-[15px] leading-[1.65rem]` (Claude's serif response type), message gap `gap-5`; **(3) Claude composer card** — `rounded-[20px] border-transparent` hairline-shadow surface, `min-h-[1.5rem] max-h-96 text-base` textarea, placeholder "Write a message…", focus firms to `bg-white/85`. Kept: model chip, send/stop, caption, header slot, measured viewport height (no page scroll), persistence + auto-downgrade + tool cards. Tests updated for the new placeholder. Docs: UI_DESIGN §17 (bubbles + composer + layout). `bun run lint` (0 errors) && `typecheck` && `test` green (958/958, 86 suites).
[x] chat follow-up — reverted the Claude **serif** response font (assistant text now uses the app sans `text-[15px] leading-relaxed`, full-width no-bubble layout kept) and gave the thread Claude's **quiet scrollbar**: `chat-scroll` class in `globals.css` (thin 8px webkit scrollbar, transparent track, rounded `rgba(0,0,0,0.15)` thumb with 2px padding, hover 0.3, Firefox `scrollbar-width: thin`) alongside `[scrollbar-gutter:stable]`. Docs: UI_DESIGN §17 (bubbles row + new Scrollbar section). `bun run lint` (0 errors) && `typecheck` && `test` green (958/958, 86 suites).
[x] chat: invisible scrollbar + manual model picker + fallback model fix — the thread scrollbar is now fully **hidden** (`.chat-scroll` in `globals.css`: `scrollbar-width: none` + `::-webkit-scrollbar { display: none }`; removed `scrollbar-gutter:stable` so no right gap) while scrolling still works. The composer's static model chip became a **manual model picker**: a native `<select>` (Sparkles + `appearance-none` + ChevronDown, `aria-label="Select model"`) listing `MODEL_CHAIN` options; selecting one updates the model, persists it (`saveChatModel`), and clears the "switched to a lighter model" notice (now driven by a new `autoDowngraded` flag so the notice only shows on automatic quota downgrade, not manual picks or restored state). **Fallback model swapped**: `gemini-2.5-flash-lite` returns 404 for new users ("no longer available… use models/gemini-3.5-flash-lite"), so `MODEL_CHAIN` = `["gemini-2.5-flash", "gemini-3.5-flash-lite"]` (verified in `@ai-sdk/google`'s model id union), `MODEL_LABELS` → "Gemini 3.5 Flash Lite". Tests updated (chat-models chain/labels, storage model keys, chat-view manual-change test + select-value assertions). Docs: UI_DESIGN §17 (scrollbar + picker), ARCHITECTURE §22, README, AGENTS, Arco_todo. `bun run lint` (0 errors) && `typecheck` && `test` green (959/959, 86 suites).
